# Project TODO

## Sprint status

Sprint 2, Brisbane Draft-EIR defense vertical slice, is active. Tasks 02 through
05G are complete and accepted; Task 05F closed with a partial outcome. Task 03J
remains immutable extraction evidence, Task 04A owns accepted review, and Task
04D remains the designated linking-dependent handoff until a replacement is
accepted. Exact outcomes belong in the completed task records.

## Next action: Task 05H planning when authorized

[Task 05G](../tasks/sprint2/05g_replay_and_extend_official_reference_links.md) is
complete and accepted with limitations: **468 links and 43 explicit nonlinks**
across all 511 references, 173 gains and zero losses compared with accepted 05F.
The fresh refactored-code replay exactly matched the reviewed links and annotations;
repeat, independent review, finalization and acceptance succeeded.

Acceptance: `acceptance05gv1-d7270d3b39b2a0de220268497ff16dc85b6406f211135f59fa3813952f072cbd`.
The [final result](specs/task05g_final_result.json) binds the pointer and Task 05H
handoff. All F1, sampled-review and text-only figure limitations remain explicit.
[Task 05H](../tasks/sprint2/05h_review_and_freeze_response_inventory.md) is the next
planning task; it has not started and its execution is not authorized. No push.

### Preserved Task 06G execution history

The [Task 06G](../tasks/sprint2/06g_replay_repaired_document_and_collection_stages.md)
accepted Phase 1 plan freezes its accepted inputs, mixed-lineage boundary,
affected stages, commands, namespaces, accounting, staged runtime identities,
allowlisted resolved specs, prelaunch recovery, fresh-attempt restart design,
supervisor-terminal readiness gate, and stop conditions.
It was accepted by Astra and approved by the user on 2026-09-11. The first
prelaunch stopped before supervisor execution on a stale repository-schema
identity binding; its `replay_v1` initial packet remains preserved. The user
authorized the bounded `replay_v2` amendment, whose preparation then exposed a
legacy wholly-fresh namespace assumption before inputs or supervisor execution.
The bounded `replay_v3` amendment correctly froze its initial packet but compact
preparation exposed a prelaunch implementation-routing defect before inputs or
supervisor execution. The v4 continuation then exposed a compact completion
hash-role mismatch, again before inputs or supervisor execution. Preserve v1-v4
evidence. The v5 continuation cleared those gates and then exposed a collection
preflight assumption that contradicted the frozen Task06A/Task04D source order.
Preserve v1-v5 evidence. Independent review approved exact once-only manifest
composition with the declared accepted order. The v6 supervisor then failed
terminally before executing a document process because the command entry still
used legacy production/PDF verification and dropped mixed-lineage bindings at
the parent/worker boundary. Preserve v1-v6, launch v6, and failed attempt-v1
evidence. Independent review approved the source-free current-writer routing and
no-clobber `replay_v7` / attempt-v2 continuation. That attempt launched and then
failed because runtime resolution paired an accepted producer ID with the v7
root and the accepted aggregate heading path invoked PDF-aware routing/table
work. Preserve the complete v7 recovery, replay, launch, and failed attempt-v2
evidence. The user authorized the bounded source-free integration repair and
fresh `replay_v8` / attempt-v3 continuation. Its recovery preflight stopped
before replay/supervisor creation when cumulative accounting attempted to hash
a preserved v7 image. Preserve the v8 failure record. The bounded repair reads
prohibited payload digests from contained sealed inventories and proceeded in
fresh `replay_v9` / attempt-v3. That replay failed terminally after 19.3009
seconds when legacy record mapping rejected the truthful Final F1
`qualified_substitute` role. Preserve all v9 recovery, replay, launch, and
attempt evidence. The bounded repair closes maintained source verification,
identity membership, and canonical provenance without changing repair policy or
source/model scope. V10 prelaunch then caught a later unconditional hierarchy
PDF-read path before launch. Preserve v10 recovery/replay; its launch and
attempt-v4 roots were not created. The explicit producer-evidence-only mode
removes PDF bookmark, page-label, and native-heading reads while recording that
evidence limitation. V11 then completed F1 mapping, hierarchy, and structure,
but the active launch watch caught reference linking pairing the structure ID
with the mapping artifact root. Preserve all v11 evidence. V12 proved that
repair and completed all six F1 process checkpoints before final worker handoff
failed because its reused content owner lacked a timing entry. Preserve all v12
evidence. The bounded v13 repair records zero only for owners not invoked in
this attempt while retaining exact six-owner closure. Its prelaunch review
caught the stale v12 tmux name before launch; preserve v13 recovery/replay. V14
then completed all six F1 owners before publication rejected the unresolved
template's older final-root allowance. Preserve all v14 evidence. V15 aligned
the checked templates, but prelaunch review caught transposed roots in the
newly frozen maintained generator. Preserve v15 recovery/replay. V16 corrected
the generator, passed preflight and independent review, and launched under
active observation. Final F1 completed; Appendix A then failed terminally after
60.5483 seconds because the accepted 06D boundary packet omitted the actual
immediate plain-form Chapter 07/09 siblings, with page-491 ownership also
conflicting with the frozen Chapter 08 extent. Preserve all v16 evidence. The
user selected the earlier plain-form boundaries and authorized the bounded v2
record-order amendment. Qualification v8 now seals four explicit Chapter 06-09
repairs with truthful same-page extents, strict content-order bounds, and zero
pending review. Replay v17 preparation was preserved before launch when review
found that the new v2 runtime owners were not all frozen prelaunch. Replay v18
froze those owners and passed Appendix A, then active observation caught a
main-document failure caused by ten block-only Task 06E child endpoints. The
bounded qualification-v17 amendment restores the already accepted complete-
subtree meaning, verifies the full predecessor inventory chain, and passes an
exact current-code source-free projection with zero undeclared differences.
Replay v19 froze every added result controller and completed Final F1, Appendix
A, and main before source-free relink preparation exceeded the compact per-file
hash budget on the accepted 39.4-MB Task 04D contract bundle. Preserve all four
v19 roots and its failed terminal attempt-v9 evidence. Replay v20 used the
maintained oversized accepted-reference pattern, passed all three document
commands and relink resolution, and then failed after publishing the first
preserved source's terminal linked candidate because downstream-only identity
construction demanded references to six process configs it did not execute.
Preserve all four v20 roots and that K4 candidate. Replay v21 permits the existing
authority-aware v3 identity shape only for downstream replay: it binds the
resolved run spec and six exact stage completions without inventing process
execution. Ordinary v4 builds remain strict. Its preflight-only receipt captured
a mistyped non-sibling attempt parent, but created no replay, launch, attempt,
progress, or supervisor artifact. Preserve that receipt. Replay v22 rejected
such root-parent mismatches, passed review, and launched attempt 11. Active
observation caught the next terminal failure only after both K4 outputs
published: the compact metadata reader omitted the resolved-spec/process-config
controls already bound by v3/v4 identities. Preserve all v22 evidence, including
its post-failure diagnostic K4 descendant. Replay v23 reconstructed those
existing versioned controls without changing their preimage, but independent
review stopped before launch because the modified storage owner was absent from
the frozen generator and production-identity inventories. Preserve v23 recovery
and replay evidence; attempt 12 remained unused. Replay v24 froze that owner,
passed review, and launched attempt 12. Active observation caught a terminal
failure after nine preserved sources succeeded and Final F1 linked: v4
process-config verification used dynamic hash roles outside the accepted compact
`config` role. Preserve all v24 evidence. Replay v25 retains all six exact
authority/digest checks under that existing semantic role. Replay v25 passed
Final F1 and the first 24 collection sources before main-document validation
exposed the legacy v1 alias schema rejecting Task 06F's already-accepted v2 FC1
caption origin. Preserve all v25 and diagnostic evidence. Replay v26 selects the
existing v2 alias schema without changing FC1 policy or rows. Independent v26
review validated the actual main products but caught the link template's stale
v1 digest/size on that v2 path. Preserve v26 recovery, replay, and diagnostic;
attempt 14 remained unused. Replay v27 refreshed the v2 seal, passed review,
completed all repaired documents and 31 linked sources, then failed at G2 because
the generic evidence-read budget was smaller than the accepted canonical files
and 35-source manifest. Preserve all v27 and diagnostic evidence. Replay v28
cleared that verifier gate and completed 33 linked sources, but attempt 15 was
stopped when global swap grew by 220,856,320 bytes because the collection loop
retained every completed full relink build. Preserve all four v28 roots. Replay
v29 releases each heavy build after retaining its terminal completion paths and
streams JSONL I/O without changing bytes. The zero-swap and all other supervisor,
source, identity, provenance, and no-clobber policies remain unchanged. V29
recovery/preparation passed, but its guarded full-corpus validation left only
251,920,384 bytes of RSS headroom before real supervisor overhead, so it was not
launched. Preserve both v29 roots. Replay v30 also releases the duplicate
upstream row graph before publishing the completed remapped build. V30
preparation is preserved without a launch. Replay v31 remaps non-FC1 rows in
place after target-index construction, retains the FC1 copying path, and closes
R6 navigation equivalence. Its guarded 35-source run peaked at 6.28 GB with
4.45 GB of headroom and zero swap growth. Final prelaunch review found that its
approximately 22.77-MB launch packet exceeded the old 16-MiB external-metadata
reserve, so v31 remains preserved without launch. Replay v32 raises that reserve
to 64 MiB, verifies the assembled packet fits it, and leaves the total 32-GiB
cap unchanged by reducing the supervisor allowance equivalently. Prepare and
independently review v32, launch the still-unused attempt 16, and watch through
terminal status.
Replay v32 then launched under active observation. All three documents and all
35 relink/downstream candidates completed with zero swap growth before
collection assembly failed terminally after 360.835 seconds: its generic
candidate lookup treated each repaired document's ordinary candidate and
downstream descendant as ambiguous despite the declared
`downstream_replay_only` mode. The bounded evidence-kind selection fix passes
focused tests. Preserve all v32 and attempt-16 evidence. A full fresh replay is
not viable under the unchanged 32-GiB cumulative cap: preserved evidence leaves
about 4.06 GB after reserve, versus 12.26 GB used by v32. Renewed review is
required for a handoff-only descendant amendment that exactly reuses v32's
verified terminal relinks, or for a resource-policy change. Do not launch until
that material dependency/provenance decision is accepted.
The reviewed recovery path is now replay v36: preserve v32-v35, correct the
per-source figure-policy leak, rebuild the three repaired document descendants
and all 35 link publications source-free, then rebuild collection/comparison.
The frozen cumulative cap is 52 GiB with 64 GiB free-space minimum, 10 GiB RSS,
and zero swap growth. Attempt 18 remains unused. Run the exact v36 prelaunch
dry path, obtain independent frozen-packet review, launch in
`er-commons-06g-replay-v36`, and actively watch the early failure window.
Execution-result acceptance and separate finalization remain pending.
V36 recovery and initial resolution passed, but the actual preparation dry run
found v32 output roots in path-dependent process templates before staging inputs
or launching. Preserve v36; attempt 18 was never created. V37 carries only the
fresh path bindings in ten v4 templates, uses attempt 19 and tmux
`er-commons-06g-replay-v37`, and retains the reviewed 52-GiB/source-free policy.
Repeat exact preparation and independent packet review, then launch and watch.
V37 passed preparation and review, completed all repaired documents, all 35
relinks, and collection publication, then failed terminally in comparison after
388.189 seconds. Preserve all v37 and attempt-19 evidence. V38 corrects only the
canonical Final F1 comparison authority projection, the exact Task 06F main
figure-policy transition ownership, and source-local namespace rebinding for
accepted navigation targets. Its runtime gate requires all 28 accepted Task 04C
links before collection publication. Refresh and independently review the exact
v38 packet, run the actual prelaunch dry path, launch attempt 20 in
`er-commons-06g-replay-v38`, and actively watch beyond the prior comparison
failure. The 52-GiB cap leaves 494,295,729 bytes above v37's observed output;
any cap change requires renewed review.
Replay v38 and attempt 20 have now succeeded terminally: all three repaired
documents, all 35 relinks, both runtime policy gates, collection publication,
correspondence, and comparison completed with return code zero and zero swap
growth. Two separately preserved finalization attempts exposed and closed the
unused-attempt ordinal assumption and prohibited-payload revalidation path.
Finalization attempt 3 passed completion-last publication under the independently
reviewed finalization-only amendment. The user accepted the execution result on
2026-09-12, and Task 06G is complete. Task 06H was subsequently accepted with
sampled-review limitations on 2026-09-17. Task 05G remains inactive
pending separate authorization.
This is an MVP
correctness, provenance, contract-closure, maintainability, and reproducibility
task, not a cybersecurity audit. The Task 06G local commit is authorized; push
remains unauthorized.

Task 06B is complete. Its [maintained interfaces](pipeline_commands.md) and
[executed inventory](specs/task06b_gate2_executed_inventory.md) preserve historical
identities, original per-source manifests, replacement membership and chunked
conversion reuse. Later tasks remain inactive.

| Task | Planned work | Status |
| --- | --- | --- |
| [06A](../tasks/sprint2/06a_freeze_recovery_and_cleanup_plan.md) | Bind accepted evidence; freeze dependency, cleanup, repair, and review plan | Complete; source-free packet reviewed |
| [06B](../tasks/sprint2/06b_refactor_pipeline_identity_and_reuse.md) | Gate 1: sealed reuse and stage identities; Gate 2: rename/remove/retain cleanup and integrated proof | Complete; both gates reviewed |
| [06C](../tasks/sprint2/06c_qualify_and_process_replacement_f1.md) | Qualify and process the selected Final F1 substitute | Complete; reusable conversion/producer handoff sealed |
| [06D](../tasks/sprint2/06d_repair_duplicate_chapter_targets.md) | Repair repeated chapter headings using topology and TOC evidence | Complete and accepted |
| [06E](../tasks/sprint2/06e_restore_missing_chapter_targets.md) | Recover missing chapters or use the TOC/children fallback | Complete and accepted; v13 maintainability cleanup equivalence-reviewed |
| [06F](../tasks/sprint2/06f_publish_caption_backed_figure_targets.md) | Publish independent exact figure-caption targets | Complete and accepted; v10 correctness and maintainability review passed |
| [06G](../tasks/sprint2/06g_replay_repaired_document_and_collection_stages.md) | Replay only affected descendants and compare complete correspondence | Complete and accepted; v38 execution and finalization succeeded |
| [06H](../tasks/sprint2/06h_review_and_accept_replacement_handoff.md) | Reuse accepted review, review changed evidence, and accept replacement handoff | Complete and accepted with sampled-review limitations |

The accepted [Task 05F partial candidate](../tasks/sprint2/05f_resolve_official_draft_eir_references.md)
accounts for 511 mentions as 295 links and 216 explicit nonlinks. It preserves
66 wrong-F1-source outcomes, 79 missing-figure outcomes, and 19 Chapter 8/9
outside-source matches as repair evidence. The umbrella also covers Appendix A
repeated chapter targets. These are comparison populations, not promises that
all references will resolve after repair.

Preserve accepted conversion/producer evidence and original source manifests
for unaffected documents. Task 06B demonstrated reuse through cleanup; filename/CLI changes must not force unchanged conversion. Reuse
human dispositions through evidence correspondence. Code removal is limited to
the approved 06B inventory; external artifact cleanup, commit, and push remain
separate actions.

## Later sequence

1. [Task 05G](../tasks/sprint2/05g_replay_and_extend_official_reference_links.md):
   consume the accepted Task 06H replacement contract, update frozen consumer
   bindings, replay only affected Task 05 stages, and qualify further exact rules.
2. [Task 05H](../tasks/sprint2/05h_review_and_freeze_response_inventory.md): final
   curator review and immutable response-inventory publication.
3. Task 07: pilot reference-case authoring and evidence review.
4. Task 08: curate, cluster, split, and freeze benchmark cases.
5. Task 09: build and freeze human evaluation.
6. Task 10: build and freeze BM25 retrieval.
7. Task 11: build and freeze target generation.
8. Task 12: calibrate the automated judge.
9. Task 13: run the locked test and close Sprint 2.

No later task is active. The [Sprint 2 plan](sprints/sprint2_brisbane_draft_eir_defense.md)
owns sprint-level sequencing; the [Task 05 umbrella](../tasks/sprint2/05_build_curator_only_response_inventory.md)
owns its curator-only inventory. The [documentation guide](documentation.md)
defines ownership of current routing versus completed execution evidence.
