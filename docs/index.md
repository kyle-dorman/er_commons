# Docs Index

This page routes humans and agents to the smallest useful set of project docs.
Start with `AGENTS.md`, then use this page to decide what to read.

## Current status

Sprint 2 is active. Tasks 02 through 05G and Tasks 06A through 06H are complete
and accepted; Task 05F closed as a partial outcome with 295 links and 216
explicit nonlinks across 511 mentions. Task 03J remains the immutable extraction
basis, Task 04A supplies accepted usability decisions, and Task 04D is the
designated linking-dependent handoff. Completed task records own exact identities
and validation evidence.

## Next action: Task 05H planning when authorized

[Task 05G](../tasks/sprint2/05g_replay_and_extend_official_reference_links.md) is
complete and accepted with limitations: **468 links and 43 explicit nonlinks**
across all 511 references, 173 gains and zero losses compared with accepted 05F.
The fresh refactored-code replay exactly matched the reviewed links and annotations;
repeat, independent review, finalization and acceptance succeeded.

Acceptance: `acceptance05gv1-d7270d3b39b2a0de220268497ff16dc85b6406f211135f59fa3813952f072cbd`.
The [final result](specs/task05g_final_result.json) binds the pointer and Task 05H
handoff. All F1, sampled-review and text-only figure limitations remain explicit.
[Task 05H](../tasks/sprint2/05h_review_and_freeze_response_inventory.md) is the next
planning task; it has not started and its execution is not authorized. No push.

### Preserved Task 06G execution history

[Task 06G](../tasks/sprint2/06g_replay_repaired_document_and_collection_stages.md)
contains the Astra-accepted and user-approved Phase 1 replay plan. Its first
prelaunch stopped before supervisor execution on a stale repository-schema
identity reference, and `replay_v1` remains preserved. The user authorized the
bounded `replay_v2` amendment, whose preparation exposed a legacy wholly-fresh
namespace assumption before supervisor execution. The bounded `replay_v3`
amendment froze the mixed-lineage rule but exposed a prelaunch routing defect
before inputs or supervisor execution. The v4 continuation then exposed a
compact completion hash-role mismatch, also before inputs or supervisor
execution. The v5 continuation cleared those gates and found that collection
preflight imposed original-manifest order instead of the frozen Task06A/Task04D
order. Independent review approved exact once-only composition with the declared
order. The v6 supervisor then failed before a document process because command
entry used legacy production/PDF verification and omitted mixed-lineage
bindings at the parent/worker boundary. The v7 attempt launched after review,
then failed because an accepted producer ID was paired with the replay root and
the accepted aggregate heading path invoked PDF-aware routing/table work.
Preserve all v7 and failed attempt-v2 evidence. The user authorized the bounded
source-free repair. Its v8 recovery preflight then stopped before replay or
supervisor creation when accounting attempted to hash a preserved v7 image;
the failure record is retained. The bounded inventory-backed accounting repair
used the no-clobber `replay_v9` attempt-v3 continuation, which failed terminally
when legacy record mapping rejected Final F1's truthful
`qualified_substitute` role. Preserve its complete evidence. The bounded
verification, identity-membership, and canonical-provenance closure passed v10
prelaunch, where review caught a later unconditional hierarchy PDF-read path
before launch. Preserve v10 recovery/replay. The explicit producer-evidence-only
hierarchy mode passed in `replay_v11` / attempt-v4. The active launch watch then
caught reference linking pairing the structure ID with the mapping artifact
root after mapping, hierarchy, and structure completed. Preserve all v11
evidence. V12 then completed all six F1 process checkpoints before final worker
handoff rejected its five-key timing map. V13 closed that timing contract but
prelaunch review caught its stale v12 tmux name before launch. V14 corrected
that binding and completed all F1 owners, but publication rejected an unresolved
template root that lagged the runtime structure-root authority. Preserve all
v13/v14 evidence. V15 aligned the checked templates, but independent prelaunch
review caught transposed assignments in the frozen maintained generator. Preserve
v15 prelaunch evidence. V16 passed preflight and independent review, then the
active watch caught a terminal Appendix A structure failure after 60.5483
seconds; Final F1 had completed and published its checkpoint. Compact diagnosis
found that accepted 06D boundary evidence omitted the immediate plain-form
Chapter 07/09 headings and conflicts with page-491 ownership. Preserve all v16
evidence. The user selected the earlier plain-form boundaries and authorized the
bounded v2 record-order amendment. Qualification v8 seals four explicit Chapter
06-09 repairs with truthful same-page extents and zero pending review. Prepare
fresh replay-v17 / attempt-v8; review preserved it before launch because the v2
runtime owners were not all frozen prelaunch. Replay v18 froze those owners and
passed Appendix A before active observation caught ten incomplete Task 06E child
subtree endpoints in the main document. Qualification v17 corrects only those
frozen endpoints and passes a current-code source-free projection. Replay v19
froze the added result controllers and completed all three document commands,
then failed source-free relink preparation when compact verification attempted
to hash the accepted 39.4-MB Task 04D contract bundle. Preserve all v19 evidence.
Replay v20 retained the frozen digest, passed relink resolution, and completed all
three documents, then the first preserved-source downstream replay rejected its
truthful lack of process-config execution references. Preserve all v20 evidence,
including its terminal K4 linked candidate. Replay v21 made the existing v3
spec-only identity shape explicit only for downstream replay, while ordinary v4
document builds still require all six resolved process-config references. Its
preflight-only receipt then captured a mistyped non-sibling attempt parent; no
replay, launch, attempt, progress, or supervisor artifact was created. Preserve
that receipt. Replay v22 added exact sibling-parent and attempt-ordinal
validation, passed review, and launched attempt 11. Active observation caught a
terminal failure after K4's linked and downstream document candidates were both
published: the compact reader omitted the resolved-spec control already bound by
v3/v4 writers. Preserve all v22 evidence, including its post-failure diagnostic
K4 descendant. Replay v23 made that reader version-aware without changing the
identity preimage, but independent review stopped prelaunch because its storage
owner was missing from both frozen code inventories. Preserve v23 recovery and
replay evidence; attempt 12 remained unused. Replay v24 froze that owner, passed
review, and launched attempt 12. Active observation caught a terminal failure
after nine preserved sources succeeded and the Final F1 linked candidate
published: v4 process-config verification used dynamic hash roles outside the
accepted compact `config` role. Preserve all v24 evidence. Replay v25 retains
the exact six authority/digest checks under that existing role. Replay v25 then
passed Final F1 and the first 24 collection sources before main-document output
validation exposed a stale v1 alias-schema binding for Task 06F's accepted v2
FC1 caption origin. Preserve all v25 and diagnostic evidence. Replay v26 selects
the existing v2 alias schema without changing FC1 policy or rows. Independent
review validated the actual main products but caught that the nested link-template
reference retained the v1 digest/size. Preserve v26 recovery, replay, and its
source-free diagnostic; attempt 14 remained unused. Replay v27 refreshed that
v2 seal and passed all repaired documents plus 31 linked sources before the
generic evidence-read budget rejected G2's accepted 649-MB table file. Preserve
all v27 and diagnostic evidence. Replay v28 cleared that verifier gate and
completed 33 linked sources, but attempt 15 was stopped on global swap growth
because the collection loop retained every full relink build. Preserve all v28
evidence. Replay v29 retained only completion paths and streamed JSONL I/O, but
its guarded full-corpus run left only 252 MB below the RSS cap before real
supervisor overhead, so it was not launched. Preserve both v29 roots. Replay v30
also releases the duplicate upstream row graph before publishing its completed
remapped build, but its prelaunch packet is preserved without launch. Replay v31
performs the non-FC1 remap in place while retaining FC1 and R6 semantics; its
guarded full-corpus run leaves 4.45 GB beneath the unchanged RSS cap with zero
swap growth. Its prelaunch review then found that the approximately 22.77-MB
launch packet exceeded the old 16-MiB external-metadata reserve, so v31 remains
preserved without launch. Replay v32 raises that reserve to 64 MiB, verifies the
actual assembled packet fits, and keeps the total 32-GiB cap unchanged. Repeat
source-free preparation and final review, launch the still-unused attempt 16,
and watch through terminal status.
Replay v32 then completed all three document commands and all 35 downstream
replays with zero swap growth before collection assembly exposed an evidence-
kind selection defect: the generic lookup treated each repaired document's
ordinary candidate and downstream descendant as ambiguous. Preserve all v32
and attempt-16 evidence. The bounded selector fix passes focused tests, but a
full fresh replay cannot fit the remaining 4.06 GB under the unchanged 32-GiB
cumulative cap after v32 consumed 12.26 GB. Renewed review must authorize either
an exact handoff-only descendant of the verified v32 relinks or a resource-
policy change before another launch.
No source/model work is needed for this continuation.
Replay v36 is the authorized full source-free correction: preserve v32-v35,
enforce the per-source figure policy in both alias construction and mention
resolution, rebuild the three repaired descendants and all 35 link candidates,
then rebuild collection/comparison under a 52-GiB cumulative cap. Attempt 18
remains unused. Its exact prelaunch packet and dry path require independent
review before launch, followed by active observation through the early failure
window. Source/model execution remains prohibited.
Its actual preparation dry run then rejected stale v32 output-root fields in
path-dependent process templates before launch. Preserve v36; attempt 18 is
unused. Replay v37 is the bounded fresh-namespace correction with ten v4
templates, attempt 19, and tmux `er-commons-06g-replay-v37`; its resource and
source-free boundaries are unchanged.
Replay v37 then completed all repaired documents, all 35 relinks, and collection
publication before comparison exposed canonical Final F1 authority projection
and accepted-navigation namespace-rebinding defects. Preserve all v37 and
attempt-19 evidence. Replay v38 is the bounded fresh descendant: it derives
Final F1 ownership from canonical record/document IDs, owns only the exact
Task 06F main-figure policy transition, and requires all 28 accepted Task 04C
links after source-local namespace rebinding. Run its actual prelaunch path and
independent review, then launch attempt 20 under active observation without
changing the reviewed 52-GiB or source-free boundaries.
Replay v38 subsequently succeeded through terminal execution, correspondence,
comparison, and handoff with zero swap growth. Finalization attempt 3 completed
the reviewed completion-last readiness publication after two preserved bounded
finalizer failures closed unused-attempt enumeration and inventory-backed
prohibited-payload revalidation. The user accepted the execution result on
2026-09-12, so Task 06G is complete. Task 06H was subsequently accepted with
sampled-review limitations on 2026-09-17; Task 05G remains inactive.
The completed replay preserves its frozen mixed-lineage boundary, source-free
integration gate, commands, namespaces, accounting, staged runtime identities,
allowlisted resolved specs, recovery evidence, and supervisor-terminal record.
This remains MVP correctness, provenance, contract closure, maintainability,
and reproducibility—not speculative cybersecurity hardening. The Task 06G local
commit is authorized; push remains unauthorized.
Task 06C's selected 756-page Final F1 conversion and producer handoff remains
sealed for eventual authorized reuse; its failed acquisition and first failed
conversion attempt remain preserved.

The [maintained command map](pipeline_commands.md) and
[executed owner map](specs/task06b_gate2_executed_inventory.md) route current
interfaces and preserve the completed 06B reuse boundary.

Read in this order:

1. [Task 06 umbrella](../tasks/sprint2/06_repair_reference_sources_and_target_index.md)
   for accepted directions, inputs, sequence, and shared boundaries.
2. [Task 06G](../tasks/sprint2/06g_replay_repaired_document_and_collection_stages.md)
   for the accepted replay boundary and Phase 2 execution gate.
3. [Task 06F](../tasks/sprint2/06f_publish_caption_backed_figure_targets.md) for
   the accepted implementation, qualification evidence, and resolved remediation
   record.
4. [Task 05F accepted partial outcome](../tasks/sprint2/05f_resolve_official_draft_eir_references.md)
   for exact upstream bindings and the failure census.
5. [Architecture](architecture.md), [data/artifacts](data_artifacts.md), and the
   specific predecessor outcomes named by that subtask.

Task 06B combines safe sealed reuse and maintained-code cleanup in two gates.
Later tasks qualify the selected Final F1 substitute, repair repeated and missing
chapter targets, publish caption-backed figures, replay affected descendants,
and reuse or renew human review. Preserving existing chunked conversion and
accepted review is an explicit acceptance requirement.

After the replacement handoff is accepted, [Task 05G](../tasks/sprint2/05g_replay_and_extend_official_reference_links.md)
owns consumer-binding updates and reference replay; [Task 05H](../tasks/sprint2/05h_review_and_freeze_response_inventory.md)
owns final curator review and immutable response-inventory publication. The
[Task 05 umbrella](../tasks/sprint2/05_build_curator_only_response_inventory.md)
retains that separate curator-only inventory boundary.

## Document roles

- [`docs/product.md`](product.md): project purpose, scope, claim boundaries,
  and success criteria.
- [`docs/architecture.md`](architecture.md): maintained package, CLI, pipeline,
  and configuration boundaries.
- [`docs/data_artifacts.md`](data_artifacts.md): external root, artifact layout,
  Git policy, and provenance requirements.
- [`docs/sprints/`](sprints/): accepted sprint scope and sequencing.
- [`docs/decisions/`](decisions/): durable accepted choices and non-promoted
  results.
- [`docs/task04_maintainer_runbook.md`](task04_maintainer_runbook.md): first-pass
  review maintenance and the Task 04A transition boundary.
- [`docs/documentation.md`](documentation.md): documentation ownership and
  editing rules.
- [`docs/todo.md`](todo.md): current queue and next action.
- [`docs/backlog.md`](backlog.md): unselected future ideas only.
- [`tasks/`](../tasks/): detailed task contracts, validation, and outcomes.

## Reading rule

Do not read every historical task record for narrow work. Use the active task's
input list and the document roles above. Read a historical task only when its
outcome or preserved evidence is an explicit input to the current task.
