# Task 05G: Replay and Extend Official Reference Links

Status: **complete and accepted with limitations on 2026-09-17.
468 links and 43 explicit nonlinks; Task 05H remains unstarted.**

## Abstract

Consume the accepted Task 06H replacement review and Task 06G mechanical
handoff through explicit, versioned Task 05 consumer bindings. Reuse accepted
05D source units and 05E relationships, replay only invalidated reference
resolver descendants, and compare every one of the accepted 05F outcomes.
Exact target identity, human usability, sampled coverage, and text-only-model
eligibility remain separate. Task 05H owns final curator review and the sole
immutable response inventory.

This contract supersedes the provisional 05G plan. The initially clean
repository base is `6c95803` (Complete Task 06H accepted review handoff).
No reset or rewrite of accepted 06G/06H code, artifacts, identities, or evidence
is allowed. Preserve planning, replay, recovery, rendering, human-review,
failed/incomplete finalization, merge, limitation, registry, handoff, and
acceptance evidence, including unaccepted and superseded attempts.

## Goal and authorization gates

1. **Phase 1, approved:** revise this contract, freeze compact metadata and exact
   reconciliation membership, add source-free fixture/regression tests, and
   obtain independent Astra review. No production implementation, replay,
   external output namespace, or acceptance pointer is created.
2. **Phase 2, authorized and complete:** implement the versioned consumer, validators,
   comparison and checkpoint behavior below using synthetic fixtures. Run
   source-free repository checks and independent correctness/maintainability
   review. Present the resolved run specification, command, roots, exact owned
   code/schema digests, and resource packet before execution approval.
3. **Phase 3, separate execution approval:** run one binding-only source-free
   replay using the accepted 05F rules and the narrowly declared F1 routing
   transition. Compare all outcomes, repeat from verified checkpoints, and
   stop with the complete census. No additional matching rule is included.
4. **Phase 4, optional rule cycle:** each proposed exact rule needs a bounded amendment,
   positive and negative controls, independent review, and explicit user
   authorization before a fresh replay. Upstream repairs require their own
   scope decision; they are not an implicit 05G loop.
5. **Phase 5, working-candidate closure and acceptance:** after comparison, tests and
   human code-quality review, separately authorize completion-last candidate
   finalization, then explicit acceptance of the exact candidate and adjacent
   pointer. Neither is the Task 05H inventory release.

No gate implicitly authorizes models, PDFs/images, rendering, extraction,
conversion, accepted-artifact mutation, cleanup, commit, push, or Task 05H.
Source access is unnecessary for the planned work. If it becomes necessary,
stop and present a bounded new request. Historical `execution_authorized:false`
fields remain unchanged; new authorization belongs to the new run request.

## Inputs and exact authority

The machine-readable [Phase 1 binding freeze](../../docs/specs/task05g_phase1_bindings.json)
is a compact planning record, not an executable specification. It records exact
acceptance fields, the complete non-executable handoff, warning bindings, and
all seven finalization-file sizes and SHA-256 digests. Paths below are relative
to `ER_COMMONS_DATA_ROOT`; no committed run config may embed an absolute root.

| Authority | Exact accepted identity |
| --- | --- |
| 06H acceptance | `acceptv1-7702aa9d77da3f3ecdc9da0a126d5b7ffc1e8483c0e8054a3ad6ad3c04d139c1` |
| 05G input handoff | `handoffv1-9ce5d7c8b5f328ae714605709f344e66ace60e09ac1bfd9b5d6f8a57cedf442f` |
| 06G mechanical handoff | `handoffv1-3603a7974be9b9dc7471dfd9a64e27468a2bdc67d44cef4944b8c24a5d60f8b9` |
| usability registry | `registryv1-a1754bf3e539bd3f1fe2bb1ed102243b00bd972fc37bf1bcc81c13e684c39f5c` |
| merged TOC decisions | `tocmergev1-3374f49f829bbf5c9bccfedf6e3980ac12393728ecd39e2158746e1fbcd4ff38` |
| sampled review merge | `reviewmergev1-d9b6301048b7ac5bc5efd0c2f2a6076362e718e799ffea48ae4b6d50ccf99b9e` |
| target limitations | `targetlimitationsv1-da1e3a7397e582d215c232c12be681dc84ded1d35fcb76063eeba61649fa7d16` |
| selected target index | `idxv1-56350e0078ec7cd1483f94ee6803bbc5c3299aa2de74df8bf3eed3ba5a3e6755` |
| selected scope | `scopev1-c9966bf1fbf87a88f8b8094806ab578f37fc646f3b22f5c552e8623e0b509be7` |
| 05D revision | `revisionv1-857ecbc97cccc24bf18808acffd9d36418f850423b6487cafb78bbaebe26e030` |
| 05E revision | `revisionv1-df6e04a7f24a79ad15dbb12f0796edcd9c9348bdd1f1db94093dd800e4091ca1` |
| 05F baseline | `rulesv1-9e67959aefc07f9ffd65605ad9d886a53022dcaccc5c9c8bed1494c41b4c0a83` |
| 05F semantic digest | `8dcd3af81b10003c49ee0588bd01a5ce8f5e778f41159bdf8b3769794dbf80ac` |

Read `pipelines/brisbane_baylands/task_06_recovery_v1/06h/accepted.json`.
Verify its exact ID and `accepted_with_limitations` state against the adjacent
acceptance completion/inventory, then follow its exact finalization ID
`reviewfinalv1-166c681736c334388bf7161af94a416ebe4030fb40eec418bdbd704a380d62e1`.
Under that finalization's `records/`, bind `usability_registry.json`,
`toc_review_decisions.json`, `decision_provenance.jsonl`,
`sample_stratum_confirmations.json`, `target_review_limitations.json`,
`task05g_handoff.json`, and `finalization_candidate.json` together. Reject
mixing any file from the incomplete earlier candidate. Validate the recorded
finalization completion digest and inventory as well as acceptance closure.

Acceptance SHA-256 is
`62083c58e04691f9566a9fe8cccd8f047fa139efa7ee18f83a5ad41fab43eccf`;
acceptance completion SHA-256 is
`aa8a8e6630080f68174e01f821a0d80dcd0889f086331d5b5508aa66cdf45019`.
The handoff's SHA-256 is
`98179492648821c3446c6266e76cc11549837072cfea37708918569ed34d6a5e`.
The immutable candidate and handoff retain their pre-acceptance status strings;
acceptance is proved by the later pointer/record, not by rewriting those files.

Follow the accepted mechanical chain to 06G v38 finalization attempt 3,
completion SHA-256
`aceb25700dc329ad0d3a1949202fdb027e3502833f5c45278654ae9ed53b2bca`,
readiness SHA-256
`3bd11f48e75ed4e1078eaacf89c4adf6edaaf64c39cab4f55911973c19bfd057`,
and correspondence completion SHA-256
`57aeec4ebf63f996f13e392df8643899c6103c6a9673b5b96c480ed08965f9ed`.
Use its selected source membership, catalog, document roots, target index,
aliases, and direct-child section relationships. Require 35 logical sources:
32 preserved, two repaired, one substituted. Do not infer publication paths
from old directory depth or pair a structure ID with a different artifact root.

The historical comparison boundary remains accepted Task 04D handoff
`handoffv1-e54a72e4bb8f9ba34888c1fc1f51424c4cc52e5f24d6700b16b462e3a659b6d1`,
completion SHA-256
`387a07d62d96e4c5aba6f1f3d51f7f9026719b0d7de1b4eece06bed9fd78bc81`.
Do not compose Task 03J links with Task 04C overlays. Task 03J extraction,
accepted Task 04 decisions, all 725 ambiguous-link exclusions and the Task 03I
K2 pages 974–983 recheck stay referenced through 06H's sealed correspondence.
Neither the 725 exclusions nor the 757 TOC rows are response-mention counts.

[05A](05a_qualify_and_profile_response_source.md) supplies the accepted native
text/geometry route and the General Response 9 cross-volume exception;
[05B](05b_define_response_inventory_contract.md) supplies stable span/unit
identity, normalized edges and sparse corrections; [05C](05c_build_response_inventory_pilot.md)
supplies the qualified producer. [05D/05D.1](05d1_remediate_full_source_inventory_findings.md)
source records and [05E](05e_build_response_relationship_graph.md)'s 1,538-edge
graph remain unchanged, including their accepted warnings. Reuse their exact
completion, inventory, semantic digest and acceptance bindings listed in
[05F](05f_resolve_official_draft_eir_references.md#exact-accepted-inputs).
05D/E historical downstream-consumer declarations are not edited: the new 05G
request explicitly authorizes read-only reuse through the accepted 06H handoff.

Routine verification hashes compact metadata only, checks contained paths,
recorded sizes, identities, membership, schema and terminal state, and trusts
sealed large-payload digests. Read selected JSON/JSONL records needed for
resolution; never hash PDFs/images or recursively scan accepted payload trees.
Missing or conflicting seals stop before any replay output.

## Frozen populations and limitations

The [population fixture](../../tests/fixtures/task05g_population_plan.json)
freezes exact baseline mention membership and reconciliation groups using
accepted records and recorded seals, without copying source text. Phase 2 must
rederive and compare membership, not merely reproduce counts. The total is 511:
295 historical links and 216 explicit nonlinks. These are preserved baseline
outcomes, not a required new link count. No reference may disappear.

| Comparison population | Required membership/count |
| --- | --- |
| Draft EIR | 509: 456 response, 42 General Response, 11 comment-authored |
| Appendix Q | 2 explicit verification outcomes, never Draft EIR links |
| Final F1 | 66 baseline repair-required mentions |
| Caption-backed figure candidates | 79 baseline missing-figure mentions |
| Whole Chapters 8/9 | 19 (8 Chapter 8, 11 Chapter 9) outside-source controls |
| Appendix A repair impacts | 2 specific inner-target mentions |
| Collisions | all 12 complete baseline candidate sets |
| Comment-authored exclusions | all 11, never official-response links |

Groups can overlap and must not be summed as a partition. The remaining
baseline failure reasons also close exactly: 19 absent targets, 6 specific
appendix targets, 2 unroutable appendix sources, alongside the categories
above. Generic Appendix F stays unroutable. The twelve collisions include seven
main number-only sets, ES.6 (7 options), normalized Section 4-11 (3), two K1
mentions (4 part-document options each), and one K2 mention (5 options).
Keep every candidate, including options that remain unresolved after replay.

The Appendix A impacts are
`mentionv1-8cab8ca69d3784b84323523fcc4a5c78d65eddbf639e46ac141699f051871f03`
and
`mentionv1-cf4b001e6f0e463fe4279c2df2f03897ef105b4a1fc2c6511ec2e446699f0e52`.
Their baseline type is `document`, but their explicit
`more_specific_appendix_target_requires_resolution` reason protects the attached
inner target. A type-only specificity check is insufficient. Protect all six
such appendix outcomes, all typed inner references, and their original context.
The protected fixture set conservatively also contains both untyped Appendix Q
outcomes; this does not classify them as specific Draft EIR targets.
Never replace a chapter by its first section, a missing subsection by its
parent, an exact figure by a truncated marker, or a specific appendix reference
by a generic document. Figure 4.8 must not match 4.8-5 or 4.8-8.

### Sampled review is not individual equivalence

Carry exactly 757 merged TOC decisions with distinct provenance:
706 evidence-proven Task 04 disposition reuses and 51 repaired-source decisions
carried through sampled-stratum confirmation. The latter are not individually
re-reviewed or substantively proven equivalent. Their four strata contain
Appendix A `not_toc=20`, `toc=5`, and main `not_toc=7`, `toc=19`; the 11 sampled
labels match accepted dispositions. The human sample covered 41 pages, not
every target. Do not revive the superseded exhaustive-review questionnaire.

The limitation ledger closes 185 targets: one Final F1 document, four Appendix A
sections, two main chapters, and 178 caption-backed figures. Preserve the six
structural sampled-boundary statuses and each exact target limitation. Of the
figures, 12 received sampled visual review and 166 were not freshly viewed.
All 178 remain unavailable as text-only evidence unless a later explicitly
scoped task changes that status. A resolved figure link may identify a target;
it does not make its image evidence available to a text-only model. An absent
limitation lookup must never default to eligible. Unchanged targets outside the
185-target ledger inherit only their actual accepted source/target policy;
absence means no fresh 06H target review, not implicit fresh approval.

### Final F1 warnings

Logical `deir_appendix_f1` now selects physical `feir_appendix_f1`, the 756-page
Final source. This is a new source, not an entity-equivalent Draft replacement.
Every one of the 66 F1 outcomes carries the exact general edition warning and
source substitution binding from the frozen handoff, whether linked or not.
Preserve both response-specific warnings with their exact unit and mention IDs:
SA-Caltrans-9 Muni section (one mention), and SA-Caltrans-6 Table 6 (two mentions).
The binding freeze contains all three full mention IDs and both full unit IDs.

Preserve the accepted statement that the other 64 mentions are not proven
Draft/Final equivalent. This is an inherited warning-scope statement, not an
arithmetic partition: the handoff also names three distinct mention IDs in two
response units, so the ID-set complement of those three within 66 has 63 rows.
Do not silently rewrite 64, drop a named warning, fabricate a 64-ID complement,
or label any unnamed mention explicitly revised or equivalent. All 66 retain
`draft_final_equivalence_proven=false`; attach extra warnings by exact IDs.

Retain every source/producer warning: revised Appendix F.1/Final EIR/May 2026
wrapper versus Final/December 2024 transportation report; April 13 versus
April 14, 2023 memo dates; only objects `12649 0` and `12168 0` via page 28
`/Thumb` covered by the structural exception; incomplete specialized-decoder
coverage; 85 Docling list-parent repairs; and zero reconstructed tables on
pages 1, 6, 7, 9, 133, 135, 436 and 732. Preserve source-substitution digest
`fa6ba267c267604ff5f3c10d064395998f9eef1a42ce1237bd8e7e8f2f450498`
and limitation digest
`092dd9fc2fc53eaa68e713d01b01bbc9c9cbe97c9249478b3c9850117679083c`.
No old Draft F1 usability decision transfers to the Final source.

## Exact replay and consumer changes

Use the [06B maintained commands](../../docs/pipeline_commands.md) and
[executed owners](../../docs/specs/task06b_gate2_executed_inventory.md).
Do not invoke historical scripts or any document/collection pipeline replay.

| Stage | Action and owned outputs |
| --- | --- |
| 05D source units; 05E graph | Reuse exact accepted records, IDs, anchors, text, edges and warnings; no producer or graph rebuild |
| Accepted 05F mention population/context | Reuse source mentions and accepted query policy; validate all 511 IDs and authorship/domain closure |
| Reference consumer preparation | New 06H acceptance-chain loader and explicit 06G target/catalog/child bindings; new source-free prepared-input receipt |
| Resolver | Replay all 511 existing mentions because target/registry dependency identities changed; only exact 05F policy plus declared F1 transition |
| Link/diagnostic/annotation descendants | Rebuild `links/draft_eir_links.jsonl`, `outcomes/reference_outcomes.jsonl`, `diagnostics/individual_diagnostics.jsonl`, warning/limitation references |
| Derived indexes/census | Rebuild `indexes/mention_to_link.jsonl`, `indexes/target_to_links.jsonl`, `diagnostics/rule_census.json`; derive solely from outcomes/links |
| Provenance/publication | New activity, dependencies, managed-file inventory, nonterminal receipt, comparison and later completion/05H handoff |

These are the complete invalidated resolver descendants. All 06G document,
collection, index, correspondence and comparison artifacts and all 06H review
artifacts are read-only inputs, not invalidated outputs to rerun.

The implemented v6 run spec and new reference record schemas are isolated in
`reference_replay_spec.py` and the new `reference_replay_*` owners. CLI dispatch
selects v6 before invoking the historical loader. This is narrower than the
Phase 1 proposal: `run_spec.py`, `reference_baseline.py`, `contract.py`, their
old schemas, identity recipes and accepted 06G/06H owners remain unchanged.
Accepted exact query helpers remain reusable. No production document owner
depends on the new human-review consumer.

The new mechanical reader replaces inferred publication roots and section
children with explicitly selected roots from the sealed mechanical chain.
New records bind the ordered roles `task05d_completion`, `task05e_completion`,
`task05f_baseline`, `task06h_acceptance`, `task06h_handoff`, `task06h_registry`,
`task06h_toc_merge`, `task06h_target_limitations`, `task06g_handoff`,
`task06g_target_index`, `task06g_source_correspondence`, and
`source_family_catalog`. The v6 schema and comparison validator enforce these
roles without weakening historical requirements. Activity and checkpoints
bind the same inputs and current code/schema/policy identities. The loader
preserves 05F's actual receipt contract and distinguishes the 06G readiness
completion from the mechanical handoff, rather than substituting their hashes.

The sole initial policy transition removes 05F's unconditional F1
`upstream_source_identity_repair_required` block only after exact logical-to-
physical substitution verification. Outer-only references can resolve one
unique selected document target. Attached inner references still require an
exact compatible target or an explicit nonlink. No new aliases, fuzzy match,
source reassignment, or broad fallback is allowed. Whole Chapters 8/9 query
only their repaired main-document whole-chapter targets; candidates outside the
routed source remain controls. Caption aliases query only accepted targets
under the 06F per-source policy; they cannot create aliases from response text.

New outcomes and derived views retain source usability, target review status,
review provenance class, warnings, structural target identity and text-only
eligibility separately. Do not squeeze these into legacy
`map_task04a_usability` / `map_visual_usability` enums. Keep the whole 185-target
ledger by sealed reference plus exact target lookup, including document candidate
IDs versus canonical target IDs proved through selected correspondence.

## Commands, namespaces and finite operating policy

The interfaces are implemented. Phase 3 enabled only `authorization.replay`;
finalization and acceptance remain false in the [v6 request](../../configs/brisbane_baylands_2025_feir_task05g_replay_v6.json).
The [resolved Phase 2 packet](../../docs/specs/task05g_phase2_launch_packet.json)
freezes the exact request bytes, code/schema pins, command, paths and limits.
The following inspection commands are read-only:

```bash
uv run er-responses validate-spec --run-spec configs/brisbane_baylands_2025_feir_task05g_replay_v6.json
ER_COMMONS_DATA_ROOT=/Volumes/x10pro/er_commons uv run python -m er_commons.response_inventory.reference_replay_launch --run-spec configs/brisbane_baylands_2025_feir_task05g_replay_v6.json --attempt 1 --print-packet
```

After separate Phase 3 authorization, record `authorization.replay=true`,
refresh/review the exact prelaunch packet, and use its `launch_command`. Its
supervised launcher is the primary all-stage route:

```bash
ER_COMMONS_DATA_ROOT=/Volumes/x10pro/er_commons uv run python -m er_commons.response_inventory.reference_replay_launch --run-spec configs/brisbane_baylands_2025_feir_task05g_replay_v6.json --attempt 1 --operation all
```

That one invocation prepares, builds, then compares. Public `er-responses`
verbs `prepare-05g`, `build`, `compare-05g`, and `validate-05g` also route through
the supervisor for v6. Each separate invocation reserves a fresh attempt;
do not run those verbs sequentially with the default attempt 1. Explicit
`--attempt 2 --resume-from 1` selects earlier sealed stages for a continuation;
it never selects a newest directory. The worker module is private and must
not be used to bypass the launcher. Validation leaves candidate checkpoints
unchanged but a public supervised validation invocation creates its own logs.

Later separately gated verbs are `finalize-05g --run-spec PATH --quality-report
PATH --review-record PATH` and `accept-05g --run-spec PATH --candidate-root PATH
--review-record PATH --accepted-by NAME --accepted-at UTC`. Before Phase 5,
prepare and independently review a separate publication command/resource
packet: the Phase 3 execution receipt does not measure later recomputation or
publication. Current Phase 2 review does not authorize those invocations.

Under `pipelines/brisbane_baylands/task_05_response_inventory/working/05g/`,
physical checkpoints are `plans/<digest>/attempt-NNN/`,
`attempts/<digest>/attempt-NNN/resolved/`,
`candidates/replayv1-<digest>/attempt-NNN/`, and
`comparisons/replayv1-<digest>/attempt-NNN/`. Immutable prelaunch packets live at
`plans/<digest>/launch-attempt-NNN.json`. Later finalizations live at
`finalizations/<candidate-id>/attempt-NNN/`; acceptance records are under
`acceptances/<acceptance-id>/`, with adjacent `accepted.json` outside their
managed closure. No `inventoryv1` namespace is created by 05G.

Supervisor logs are disjoint siblings under
`working/05g_execution_attempts/<digest>/attempt-NNN/`, as required by the
maintained supervisor. All prior logs across identities and all new 05G
outputs count toward the same cumulative cap. Preserve failed attempts.
Candidate behavior identity excludes attempt ordinal, timestamps, logs and
separately gated authorization flags. The field `run_spec_sha256` in stage
bindings names this behavior digest; immutable launch packets additionally
seal the full original request bytes and their SHA-256, including authorization.
Changing behavior, inputs, code or schema creates a new identity; changing
only authorization does not make a reviewed candidate unreachable.

Frozen Phase 3 limits: one worker, two CPU threads, 4 GiB process-tree RSS,
zero global swap growth, 1,800 seconds per invocation, 2 GiB cumulative new 05G
output including failed attempts/logs, and at least 8 GiB free before launch.
No network, model, PDF/image access or render subprocess is permitted. Freeze
`OMP_NUM_THREADS=2`, `OPENBLAS_NUM_THREADS=2`, `MKL_NUM_THREADS=2` in the launch
packet. Use the maintained background-execution supervisor with its actual
help-validated arguments, sampled every 0.1 seconds (disk every second), in tmux
`er-commons-05g-<plan-digest-prefix>-attempt-NNN`; retain command, PID-tree,
terminal return code, RSS, swap baseline/delta, duration and byte accounting.
Actively observe launch and early progress; a quiet process is not success.
Count existing new-05G output before every retry. The separate 06G 52-GiB
historical cap and evidence are untouched. Do not raise limits or delete
preserved outputs to fit; stop for a reviewed amendment. The 05F contract had
no frozen resource numbers, so these remain conservative bounds rather than claims about corpus
performance. Phase 2 synthetic measurements are recorded below.

## Restart, comparison and acceptance design

Checkpoint only complete source-free stages (prepared inputs; resolved rows;
indexes/census; comparison). A checkpoint binds exact input IDs/digests,
implementation/policy/schema/spec, managed paths/sizes and output digests.
Write privately, validate exact file closure and semantic invariants, then
publish its completion last. Use atomic same-filesystem rename for the owned
new path; never atomically replace an accepted candidate. Log resource stops
and preserve incomplete attempt files and failure records. No terminal
completion is written on error, cancellation, timeout or limit violation.

On restart, verify the preceding checkpoint and reuse it only when all bindings
and closure match; execute only its uncompleted descendants in a new attempt.
A complete candidate repeat is read-only and returns the same identity/digest.
Interrupted or partial output cannot impersonate a checkpoint. Unexpected files,
stale code, missing warnings, changed populations, duplicate/conflicting rows,
or mismatched digest stop rather than silently repair, prune or merge outputs.
Do not carry over 05F's exception cleanup that removes staging failure evidence.

Comparison must contain one row per frozen mention ID with before/after
outcome, reason, rule, source route, target type/ID, full collision options,
warning IDs, review class, usability and text-only status. Join via unchanged
mention/span/unit IDs; prove the 05D/E identities and records remain referenced
unchanged. Normalize target namespaces only through sealed correspondence;
never normalize away source substitution, target extent/type or a warning.
Classify each delta as namespace-only, accepted source substitution, accepted
structural repair, accepted figure-target availability, review annotation, or
separately approved exact-rule change. An unexplained delta fails acceptance.
Show all 295 old links and 216 old nonlinks, gains/losses, each named population,
all collision options, complete residual census, and bidirectional index closure.
No minimum improvement count substitutes for correctness.

Candidate closure requires 511 unique outcomes, disjoint link/nonlink sets,
one compatible link per linked mention, an explicit reason for each nonlink,
no dangling link/diagnostic, exact forward/reverse reconstruction, no unexpected
source/domain membership, exact warning/limitation coverage, verified checkpoints,
and deterministic semantic reproduction. 11 comments and 2 Appendix Q outcomes
remain nonlinks. Every changed link/collision and usability/limitation change
must be inspected from sealed records before working-candidate acceptance;
review of source fidelity requiring PDF/images belongs to a later authorized
gate, not an invented review claim here.

The eventual completion binds exact inputs, policy/code/schema, authoritative
new output digests, comparison, resource receipt, independent review and quality
report. Status is `complete_with_limitations`, not benchmark eligibility.
An adjacent explicit user acceptance record binds the candidate completion and
inventory, remains outside managed closure, and designates 05H as consumer.
Missing/conflicting review decisions block finalization; repeat acceptance is
idempotent only for the exact same content. A different decision/candidate
requires a fresh record and renewed approval.

## Focused source-free tests and validation

Phase 1 adds planning fixtures and regression guards over existing accepted
helpers. They do not establish that an unimplemented 05G pipeline works.
Phase 2 must add the following executable tests against the new owners before
any real replay; use small synthetic JSON/JSONL and temporary directories only.

| Requirement | Positive and failure controls |
| --- | --- |
| Acceptance binding | Exact accepted 06H chain passes despite unchanged pre-acceptance candidate labels; reject mechanical-only handoff, absent pointer, wrong completion/registry/TOC/limit digest, mixed incomplete finalization |
| Population closure | Exact 511-ID set and 295/216 baseline partition; reject same-count ID replacement, duplicates, missing rows, wrong authorship/domain, missing reconciliation member or collision option |
| Warnings | All 66 general warnings; exact two units/three IDs; preserve literal 64 separately; reject dropped source warning, changed dates/exception scope, equivalence promotion or fabricated complement |
| Specific targets | Preserve both Appendix A impacts and all six attached-specific nonlinks despite document type; reject chapter-to-section, subsection-to-parent, figure-prefix and specific-to-document fallback |
| Sampled limitations | Exact 706 proved/51 sampled provenance; four strata/11 labels; retain 185 targets, 12/166 figure coverage and all 178 false text-only flags; reject promotion by alias, visual sample or missing lookup |
| Restartability | Interrupt each stage before completion, preserve evidence, resume exact sealed predecessors; complete repeat is read-only; reject stale code/spec/input, partial receipt, extra file or changed checkpoint output |
| Conflicting/incomplete outcomes | Reject both link and nonlink for one mention, dangling reverse entries, absent diagnostic, conflicting review, unowned delta and publication before closure |
| Source-free operation | Fail if PDF/image/model opening, renderer, network or large-input hash path is called; synthetic stress test resource stops and completion suppression |
| Historical compatibility | Accepted v5 fixtures/readers and exact-rule outcomes remain unchanged; v6 roles cannot leak into historical recipes |

Use `uv run pytest tests/test_task05g_planning_guards.py` for the current guard
suite, and `uv run pytest tests/test_task05g_population_plan.py` for the frozen
population and metadata projections. Phase 2 runs the new consumer/acceptance/comparison tests plus existing
05F and 06H focused suites, `make fix`, `make check`, and `git diff --check`.
Require independent Astra review of the completed Phase 1 plan and remediation
of material findings. Before execution require a separate independent review
of actual Phase 2 code and the resolved launch packet, including readable
ownership, errors, debugging/restart instructions and finite limits.

## Task 05H handoff and non-goals

Publish a compact working-link-layer handoff only after explicit acceptance.
It names accepted 05D/05E revisions, the 05F baseline, exact 06H acceptance and
all selected review/mechanical authorities, the new 05G candidate/completion,
full before/after census and membership, residual nonlinks/collisions, warning
and limitation references, independent reviews and read-only validation command.
It also carries 05D/05E warnings, all 725 inherited exclusion correspondences,
the Task 03I recheck, the 706/51 distinction and the unchanged text-only figure
boundary. `task05h_execution_authorized=false` remains explicit.

Revise 05H from that actual result; it owns curator review, sparse dispositions,
correction routing, and the sole immutable `inventoryv1` release. Do not add
Volume 4 to the model corpus, extract Appendix Q, infer response adequacy or
benchmark eligibility, cluster, split, author cases, or build a workflow engine.
Older 05A–05B/umbrella references assigning final publication to 05G are
historical sequencing; current publication ownership is 05H.

## Research / learning checkpoint

Consulted the maintained 06B maps and accepted 05A–05F/06G/06H/04D outcomes.
[W3C PROV-O](https://www.w3.org/TR/prov-o/) distinguishes derivation and revision
from identity: this supports explicit substitution/correspondence without
inventing Draft/Final equivalence. Adopt that distinction in compact existing
JSON contracts; no RDF library or provenance framework is needed.
[Python's os.replace documentation](https://docs.python.org/3/library/os.html#os.replace)
provides atomic rename semantics when successful. Atomic visibility alone does
not prove a complete multi-file artifact; retain validated inventory and
completion-last publication with no-clobber accepted namespaces.

The main learning is that acceptance is a separate bound event. A target can
be mechanically exact, human usable with sampled limitations, and unavailable
to a text-only model at the same time. Replaying a consumer must preserve all
three facts rather than collapse them into a single success flag.

## Phase 1 review and outcome

The independent Astra Phase 1 review passed on 2026-09-17 with no material
findings. It checked the accepted compact metadata against the binding freeze,
exact population closure, warning and sampled-review semantics, consumer
ownership, commands, restart, comparison, finite limits and authorization gates.
The minor observation that the protected set conservatively includes two
untyped Appendix Q outcomes is now explicit above. Astra also rechecked the
final coverage projection and seventh population test: all 185 target rows,
four strata and 706/51 provenance counts match accepted metadata; PASS remains
valid with no material findings.

Validation: 23 new planning/fixture guards and seven existing 06H contract tests
pass (30 total); Ruff lint/format checks and `git diff --check` pass. These are
source-free tests over synthetic temporary artifacts and checked-in metadata,
not an implemented or executed 05G pipeline. The full repository check is a
Phase 2 implementation gate; no production code changed in Phase 1.

The acceptance and finalization metadata seals were checked read-only. The
initial repository was clean at `6c95803` and HEAD remains there. No model,
PDF/image, extraction/conversion, 05G/05H replay, accepted-artifact mutation,
acceptance publication, commit or push occurred. Approval of this plan alone
does not authorize implementation or execution; the next requested gate is
Phase 2 source-free implementation and synthetic validation.

## Phase 2 authorization

The user authorized “run phase 2 and stop.” This permits implementation and
synthetic validation, not real-population resolver execution, external Task 05G
output creation, finalization or acceptance. Compact accepted metadata may be
read to freeze the future run request and command packet.

## Phase 2 outcome

The versioned input reader, mechanical bindings, resolver, comparison,
checkpoint storage, supervisor launcher, workflow and later gated acceptance
owners are implemented. Source-free fixtures cover acceptance closure,
511-outcome membership, F1 warnings and physical routing, sampled limitations,
specific-target protection, interruption/resume, deterministic recomputation,
conflicting review, idempotent acceptance and public CLI failure exit status.

A synthetic 511-reference resolver probe took 0.478 seconds, peaked at
168,345,600 bytes RSS and serialized 3,722,232 bytes. Harmless subprocess tests
also exercised actual supervisor timeout and output-limit termination without
terminal stage completion. These measurements do not establish real-population
runtime or successful accepted-input replay.

All 152 focused Task 05G tests pass. `make fix` passes; `make check` passes
formatting, lint and mypy, with 2,257
pytest passes and three pre-existing Task 06G failures. Those three unchanged
tests expect finalizer drift, but the historical generator rejects the current
accepted HEAD earlier on repository-base mismatch. They were reproduced from
an isolated export of accepted commit `6c95803`. No historical code, recipe,
test or accepted evidence was modified to conceal that incompatibility.

The independent Phase 2 review and remediations are recorded in
[the review note](../../docs/specs/task05g_phase2_review.md). The resolved launch
packet is an inspection artifact only. No external 05G namespace, real resolver
run, PDF/image/model work, extraction/conversion, acceptance pointer, Task 05H
execution, commit or push was performed. Phase 3 needs separate explicit user
execution authorization; stop here.

## Phase 3 authorization

The user authorized “ok do phase 3.” Enable only the replay gate and execute
the reviewed all-stage source-free replay, comparison and checkpoint repeat.
Finalization, acceptance, new matching rules, Task 05H, commit and push remain
unauthorized. Preserve the Phase 2 packet and all subsequent attempt evidence.

### Preserved Phase 3 attempt 1 and bounded correction

The independently reviewed authorization-only launch used behavior
`b1b2faa1d3c0d43aa0b6af18266487b27deda8fc0150d8101c87a78a994aefff`, attempt 1.
Preparation and candidate checkpoints completed, then comparison correctly
rejected missing global diagnostic candidates for four protected specific
appendix references. Terminal supervisor status is failed (return code 1),
4.123 seconds, 585,580,544 bytes peak RSS and zero swap growth. Preserve all
checkpoints, the full original request/packet, logs and failure receipt.

The localized correction keeps exact global/source lookup diagnostics while
retaining zero compatible generic document targets and the specific nonlink.
It adds no matching rule and leaves the strict comparator unchanged. Independent
record inspection identified exactly four affected diagnostic rows (7, 7, 16,
16 baseline global options). Regression controls cover outside-source options
and direct document aliases without permitting a generic link. Because code
identity changed, attempt 2 uses a fresh behavior namespace with no checkpoint
reuse across identities. All prior bytes remain in cumulative accounting; caps
and publication gates are unchanged. The original Phase 3 packet is preserved;
the new packet is `docs/specs/task05g_phase3_attempt2_launch_packet.json`.


## Phase 3 outcome

The [execution summary](../../docs/specs/task05g_phase3_result.json) seals the
actual supervisor receipts, checkpoints, comparison and repeat evidence.
Successful attempt 2 uses behavior
`463a13c8e7314cd534001f3f099078686198eecb1250c60900b532e7ab74a9a1` and candidate
`replayv1-463a13c8e7314cd534001f3f099078686198eecb1250c60900b532e7ab74a9a1`.
Its semantic digest is
`31bcd2f3f3cc35ea2f82ac9a45879bfe54021646a011db3b10e170b45a754607`.

All 511 baseline outcomes remain represented. The new census is **450 links
and 61 explicit nonlinks**, versus 295/216 before: **155 gains and zero losses**.
The complete comparison retains every before/after row and classifies 315 as
namespace-only, 33 as review annotation, 66 as accepted source substitution,
78 as accepted figure-target availability and 19 as accepted structural repair.
These are comparison classifications, not newly asserted semantic equivalence.

| Reconciliation population | Links | Explicit nonlinks |
| --- | ---: | ---: |
| Final F1 (66) | 58 | 8 |
| Figure mentions (79) | 78 | 1 |
| Whole Chapters 8/9 (19) | 19 | 0 |
| Appendix A protected impacts (2) | 0 | 2 |
| Collision sets (12) | 0 | 12 |
| Comment-authored exclusions (11) | 0 | 11 |
| Appendix Q (2) | 0 | 2 |

Populations overlap. The 61 residuals are 19 absent exact targets, 14 specific
appendix targets still requiring resolution, 12 exact collisions, 11 comment
exclusions, two unroutable appendix sources, two Appendix Q verifications and
one absent exact figure. All 66 F1 warnings remain; all 178 caption-backed
figures remain unavailable as text-only evidence. The 706 evidence-proven
reuses and 51 sampled-stratum decisions retain distinct provenance.

Attempt 2 completed in 4.150 seconds, peak RSS 559,431,680 bytes, zero swap growth.
Attempt 3 explicitly resumed attempt 2 under the same identity and reran all
stage checks in 3.124 seconds, peak RSS 561,561,600 bytes, zero swap growth.
Every original checkpoint file retained the same bytes and modification time;
comparison repeated deterministic resolution without replacing checkpoints.
All attempts, failed evidence and operational logs total 38,606,592 bytes,
below the unchanged cumulative 2-GiB cap.

All 153 focused Task 05G tests pass. `make fix` passes; full `make check` passes
format/lint/mypy with 2,258 passing tests and the same three historical 06G
failures reproduced at the accepted base during Phase 2. `git diff --check`
passes. Independent prelaunch review approved the authorization-only first
packet, bounded diagnostic correction/new packet, and explicit repeat packet.
The independent [Phase 3 result audit](../../docs/specs/task05g_phase3_review.md)
passed with no material findings.

Phase 3 is complete; this is a working candidate requiring later review, not an
accepted inventory. No PDFs/images/models, extraction/conversion, accepted
artifact mutation, finalization, acceptance pointer, Task 05H, commit or push.
Phase 4 is optional and needs authorization for any new rule; Phase 5 requires
separate publication/resource review and explicit finalization/acceptance.

## Phase 4 bounded general-rule trial authorization

The user authorized trying the low/moderate specific-appendix cases with general
solutions and reporting cases that cannot be handled without document-specific
exceptions. This permits a fresh source-free rule cycle and comparison, not
finalization/acceptance. Preserve Phase 3 and all earlier attempts. Leave the
19 missing-exact-target cases and three figure-reference cases unchanged.

Implement a separately selected general-inner-reference policy: complete-token
specificity detection, explicit compound references scoped to the selected
appendix, exact identifier/title matching and uniqueness. No mention IDs,
document names or desired counts in matching rules; no fuzzy or semantic title
selection. Repeated table identifiers remain nonlinks unless exact syntax
provides enough disambiguation. Investigate existing caption/page qualification
without inventing upstream target identities. If unavailable or ambiguous,
defer and report rather than broadening to the whole document.

Freeze the Phase 3 candidate completion as an additional comparison basis and
the exact 14 investigated mention IDs as an allowed-change review population
(not a resolver allowlist). Require unchanged target semantics outside it,
unchanged existing links, warnings and eligibility, exact 511 closure, and
before/after evidence for every trial change. Add positive/negative synthetic
controls; independently review actual code and a fresh launch packet before
execution. Use unchanged source-free supervisor limits and cumulative accounting,
a fresh identity/attempt, and explicit checkpoint repeat. No models, PDFs/images,
extraction/conversion, accepted-artifact mutation, acceptance, commit or push.

### Phase 4 preserved attempt 4 and bounded scope correction

Independent review approved the general parser, supplemental table/page aliases
and prior-result comparison. Attempt 4 completed a working candidate but failed
the strict Phase 3 comparison: applying the new parser to every outer reference
also reinterpreted existing links outside the fourteen investigated nonlinks.
Preserve its packet, checkpoints, logs and failed receipt; no acceptance or
comparison completion was published. The nine potential gains were not accepted
as a successful result because the trial also changed out-of-scope outcomes.

The correction applies new proposals only when the existing general inner-target
guard would have blocked a document link. This is a reusable rule-predicate
boundary, not an allowlist of document names or mention IDs. The independent
fourteen-ID comparison remains a separate scope check. Existing links are not
reinterpreted in this nonlink-remediation trial. Attempt 5 uses a new identity
with no cross-identity checkpoint reuse, under the same cumulative resource cap.

Supplemental aliases are consumer-derived evidence over existing sealed page/
table identities. Unique footer labels require exact source-wide uniqueness;
letter-prefixed table captions retain strict existing same-section adjacency
and geometry. Contradictory aliases are retained as ambiguity evidence. Upstream
records remain unchanged. Table 5 lacks an exact title/page discriminator and
Table 6 fails the strict geometry rule; no exception or tolerance is introduced.


## Phase 4 outcome

The [result manifest](../../docs/specs/task05g_phase4_result.json) seals attempts
4–6, all four successful checkpoints, both comparisons and the repeat snapshot.
The [review note](../../docs/specs/task05g_phase4_review.md) records independent
review, remediations, successful cases and deferred cases.

Attempt 5 uses behavior
`abb4b6df8f79537dabe3fad564b165d87cd6c249d2dcb498070d20e88936e6bb` and candidate
`replayv1-abb4b6df8f79537dabe3fad564b165d87cd6c249d2dcb498070d20e88936e6bb`.
Its semantic digest is
`792dd533ec2b5e03747bc202538bca368668bfff2f95b0462ea939155652ad3d`.

The result is **459 links and 52 explicit nonlinks across all 511 outcomes**:
nine gains over Phase 3, zero losses, and all 497 outcomes outside the fourteen
reviewed cases unchanged. Every prior link and its annotations remains intact.
Relative to accepted Task 05F's 295/216 population, this is 164 gains and zero
losses. The fourteen-case group now has nine links and five nonlinks: two
unresolved table cases and three untouched figure cases. All 19 missing-exact-
target cases remain unchanged, following the user's disposition.

All 66 Final F1 warnings are preserved, including both response-specific
revised-content warnings and the rule that the other 64 mentions are not proven
Draft/Final equivalent. All 178 caption-backed figure targets remain unavailable
as text-only evidence. The 706 evidence-proven Task 04 reuses remain distinct
from 51 decisions carried through sampled-stratum confirmation; those 51 are
not individually re-reviewed or substantively proven equivalent.

Attempt 5 succeeded in 5.938 seconds, peak RSS 670,023,680 bytes. Attempt 6
explicitly resumed attempt 5 and repeated all stages in 4.759 seconds, peak RSS
682,065,920 bytes. Both had zero swap growth. Every original checkpoint file
retained identical bytes and modification time. Cumulative artifacts and logs
occupy 77,946,736 bytes under the unchanged 2-GiB cap. Failed attempt 4 remains
preserved, including its out-of-scope comparison rejection.

All 214 focused Task 05G tests pass. `make fix` passes. Full `make check` passes
formatting, lint and mypy; pytest reports 2,319 passes and the same three
historical Task 06G failures reproduced at accepted base `6c95803` in Phase 2.
Independent prelaunch and actual-result review passed without outstanding
material findings. No source/model execution, accepted-artifact mutation,
finalization, acceptance pointer, Task 05H, commit or push occurred. Phase 4's
bounded trial is complete; Phase 5 publication remains separately authorized.


## Post-Phase 4 collision diagnosis

The user requested diagnosis of the twelve unchanged exact collisions. The
[diagnosis](../../docs/specs/task05g_collision_diagnosis.md) records nine section
mentions whose competing targets strongly match repeated page-header exemplars,
and three whole-report references spanning four-part K1 or five-part K2.
Header qualification is a plausible general repair; multipart references need
an explicit aggregate-target/link contract. No repair or replay was performed.
Existing accepted hierarchy provenance is retained; metadata diagnosis is not
new visual review or target acceptance. The candidate remains 459 links/52
nonlinks, with all prior limitations and publication gates unchanged.

## Phase 4 header-qualification trial authorization

The user authorized trying general header qualification for the nine diagnosed
section collisions. Preserve the completed inner-reference trial and all earlier
attempts. The new policy includes the existing inner rules unchanged and adds
collision-only section qualification using existing canonical page-header
exemplars, exact normalized text, matching page dimensions and bounding boxes
within one point on a different page. Require exactly one remaining target;
otherwise retain the original nonlink and candidate diagnostics. This is an
inference from extraction evidence, not fresh human review or canonical repair.

Bind attempt 5's exact candidate completion as the previous result and the nine
section mention IDs as comparison scope only. Require all 502 other outcomes,
all existing links, all warnings and review limitations unchanged. The three
multipart collisions remain out of scope. Use synthetic positive/negative tests,
independent code/packet review, fresh supervised attempt 7 and explicit repeat
attempt 8, with existing resource limits and cumulative accounting unchanged.
No PDF/image/model access, extraction, accepted-artifact changes, finalization,
acceptance, Task 05H, commit or push is authorized by this trial.


### Header trial outcome

Attempts 7 and 8 (explicit resume/repeat) succeeded under behavior
`44763b4cce241c7b07562f5b7f7f18da8ea01f87385420dbacdca69b9690f3c0`.
The [review and outcome](../../docs/specs/task05g_header_review.md) and
[result manifest](../../docs/specs/task05g_header_result.json) bind exact
checkpoints, comparison, supervisor receipts and repeat snapshot.

The working candidate now has **468 links and 43 explicit nonlinks**: nine gains,
zero losses and all 502 other outcomes unchanged relative to attempt 5.
All twelve collision cases were preserved in the diagnosis; nine section
references now resolve and three multipart document references remain nonlinks.
All nineteen missing-exact-target cases, five remaining specific appendix
nonlinks, F1 warnings, sampled-review limitations and figure text-only exclusions
are unchanged. The rule is a consumer qualification over existing evidence;
canonical headings, section extents and accepted review records are not repaired.

272 focused tests pass. Full `make check` passes format/lint/mypy and 2,377 tests,
with the same three known historical Task 06G failures. Independent code, packet
and actual-result review passed without material findings. The parent verified
repeat completion and unchanged checkpoint bytes/mtimes. Peak RSS was below
720 MB, zero swap growth; cumulative output is 102,373,277 bytes. No PDFs/images,
models, extraction/conversion, accepted-artifact mutation, finalization,
acceptance pointer, Task 05H, commit or push. Stop before further rule cycles or
separately authorized Phase 5 publication.


## Multipart collision disposition

The user accepted leaving the three remaining multipart appendix references as
nonlinks for now: K1 in M-OSEC-91 and M-OSEC-342, and K2 in M-OSEC-91. No
multipart target support or resolver change is requested. Preserve the original
collision evidence and the current 468-link/43-nonlink working candidate.


## Comment-authored exclusion diagnosis

The [eleven-case diagnosis](../../docs/specs/task05g_comment_exclusions_diagnosis.md)
confirms all mention spans lie within accepted comment units, across ten
comments. Eight have a unique indexed target; three LAFCo comments call section
4.16 “Chapter 4.16” and have no exact chapter match. All remain intentionally
excluded from official-response links under the accepted 05F/05G authorship
boundary. No rule change or replay was performed; the candidate stays 468/43.


## Unroutable appendix diagnosis

The [two-case diagnosis](../../docs/specs/task05g_unroutable_appendix_diagnosis.md)
finds both labels are bare Appendix F, absent from the exact catalog. M-CSSC-32
explicitly cites inner Appendix F of F.1; M-CSSC-135 cites transportation reports
and inner C.4. Candidate inner targets exist in selected Final F1, but safe
routing needs compound/family rules, with duplicate-label controls for C.4.
Both mentions are outside the original 66 F1 warning population; any repair
requires explicit additive warning coverage while preserving accepted warnings.
No repair or replay was performed; the working candidate remains 468/43.


## Human-maintainability pass before Phase 5

The user requested an MVP code-quality pass prioritizing human readability,
editability, understanding and debugging. The
[review](../../docs/specs/task05g_code_quality_review.md) records bounded resolver
and input-reader refactors, explicit named arguments/shared policy definitions,
actionable failure context and stage/reuse logging. The
[maintainer guide](../../docs/specs/task05g_maintainer_guide.md) maps responsibilities
and debugging steps. No new matching rules, frameworks or dependencies were added.

Independent review passed after closing the read-only audit script's root-binding
finding. The supervised [equivalence proof](../../docs/specs/task05g_quality_equivalence.json)
reproduces all 511 outcome/link/diagnostic/index rows, census and full comparison
byte-for-byte at 468 links/43 nonlinks, using the historical activity only for
comparison and current code hashes as separate verification provenance.
All prior checkpoint bytes/mtimes remain unchanged. 272 focused tests pass; full
checks have 2,377 passes and the same three historical 06G failures. No acceptance, finalization,
Task 05H, commit or push occurred. Before Phase 5 publication, prepare a fresh
current-code request and separately authorize its supervised replay/comparison;
historical requests retain original pins and evidence. Link recovery is finished.

## Phase 5 conditional execution and closure authorization

The user explicitly authorized a fresh replay with the refactored code, followed
by acceptance, local commit and task closure only if links match the existing
468-link/43-nonlink result. Any mismatch requires stopping. Bind attempt 7 as the
prior candidate, retain the nine-case runtime scope guard, and additionally
require exact equality of all 511 outcome records except content-addressed
outcome/link IDs, plus link-record equality except activity/link IDs. This
protects every warning, target annotation and remaining nonlink, not only counts.

Use fresh attempt 9 under unchanged resource limits, preserve all earlier
artifacts and requests, and independently review the actual prelaunch packet
and result. Approval flags remain replay-only until the strict comparison passes;
then a separate publication request may enable finalization and acceptance under
the same behavior identity. Task 05H and push remain unauthorized.


## Accepted Task 05G outcome

The user authorized acceptance and local commit conditional on an exact fresh
replay match. Attempt 9 with the refactored code succeeded, and independent
review confirmed all 511 outcomes, 468 links and 43 diagnostics equal the prior
candidate except new content/activity identities. Every target, warning and
review annotation is unchanged. The [strict comparison](../../docs/specs/task05g_final_equality.json)
records that condition; repeat attempt 10 succeeded with unchanged checkpoint
bytes and modification times. Compared with accepted 05F, this is 173 gains and
zero lost links. Link recovery is closed at this accepted partial outcome.

- Candidate: `replayv1-2f17f8c6c701e0811e6a4674b155d688b95e86521d83e53bafd86c722e54abb3`.
- Acceptance: `acceptance05gv1-d7270d3b39b2a0de220268497ff16dc85b6406f211135f59fa3813952f072cbd`.
- Acceptance pointer: `/Volumes/x10pro/er_commons/pipelines/brisbane_baylands/task_05_response_inventory/working/05g/accepted.json`.
- Exact receipts, checkpoint seals and Task 05H handoff:
  [final result](../../docs/specs/task05g_final_result.json).
- Finalization review and quality closure: [review](../../docs/specs/task05g_final_review.json)
  and [quality](../../docs/specs/task05g_final_quality.json).
- Current accepted publication request:
  `configs/brisbane_baylands_2025_feir_task05g_accept_v6.json`; the separate
  replay-only request and original launch packets remain preserved.

Supervised finalization and acceptance both succeeded with zero swap growth.
The sole acceptance pointer names the exact completion-last finalization and its
Task 05H handoff; it does not execute Task 05H or publish the final inventory.
All 706 evidence-proven reuses, 51 sampled-stratum carries, 66 F1 warnings
(including the two revised-content warnings and other-64 limitation), and 178
figure text-only exclusions remain explicit. All failed/incomplete and superseded
05G attempts and all accepted 06G/06H evidence are preserved.

Validation remains 272 focused tests passing and 2,377 full-suite passes, with
three documented historical 06G failures reproduced at accepted base. Formatting,
lint, mypy and diff checks pass. No PDF/image/model access, extraction/conversion,
accepted upstream mutation, or push. Local commit and task closure are authorized.
Task 05H may now be planned from this accepted handoff under separate authorization.
